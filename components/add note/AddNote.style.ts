import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "red",
        position: "absolute",
        right: 30,
        borderRadius: 50,
        padding:25,
        bottom: 170,
        zIndex: 100
    }
})